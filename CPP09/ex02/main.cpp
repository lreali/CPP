#include <iostream>
#include <string>

int main(int argc, charr **argv)
{
	if (argc < 2)
		std::cout << "Nothing to sort" << std::endl;
		// parse for only positive number
	// check sort fonction
	// create your own algorithme merge insert sort
	// compare both container

	return (0);
}
